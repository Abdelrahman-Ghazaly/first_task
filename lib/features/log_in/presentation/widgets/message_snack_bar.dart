import 'package:flutter/material.dart';

class MessageSnackBar extends StatelessWidget {
  const MessageSnackBar({Key? key, required this.message}) : super(key: key);
  final String message;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
